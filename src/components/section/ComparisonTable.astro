---
import { list, resolveProps, style } from 'meno-astro';
import Heading from '../ui/Heading.astro';
import Section from '../ui/Section.astro';
import SectionHeader from '../ui/SectionHeader.astro';
import Subtitle from '../ui/Subtitle.astro';
import Text from '../ui/Text.astro';

const __props = resolveProps(Astro, {
  subtitle: { type: "string", default: "Compare" },
  title: { type: "rich-text", default: "Your section heading" },
  text: {
    type: "rich-text",
    default: "Short supporting description that previews how this section looks and reads in the editor."
  },
  column1Title: { type: "string", default: "Basic" },
  column2Title: { type: "string", default: "Pro" },
  items: {
    type: "list",
    itemSchema: {
      title: { type: "string", default: "Feature name" },
      value: { type: "string", default: "-" },
      label: { type: "string", default: "Yes" }
    },
    default: [
      { title: "Feature one", value: "Basic", label: "Included" },
      { title: "Feature two", value: "Limited", label: "Included" },
      { title: "Feature three", value: "-", label: "Included" },
      { title: "Feature four", value: "-", label: "Included" },
      { title: "Feature five", value: "-", label: "Included" }
    ]
  },
  theme: { type: "select", default: "primary", enumName: "theme" }
});
const {
  subtitle,
  title,
  text,
  column1Title,
  column2Title,
  items,
  theme,
  class: className,
} = __props;
---
<Section theme={theme} spaceTop="default" class={style({}, __props, { root: true })}>
  <SectionHeader isCenter={true} class={style({ base: { maxWidth: "600px" }, tablet: {}, mobile: {} }, __props, { instance: true })} __menoStyle={{ base: { maxWidth: "600px" }, tablet: {}, mobile: {} }}>
    {subtitle && (
      <Subtitle text={subtitle} fade="0" align="center" />
    )}
    <Heading text={title} fade="0.2" align="center" size="2" tag="2" />
    {text && (
      <Text text={text} size="large" fade="0.4" align="center" />
    )}
  </SectionHeader>
  <div class={style({ base: { overflowX: "auto" } }, __props)} fade={0.4}>
    <table class={style({ base: { width: "100%", borderCollapse: "collapse", minWidth: "500px" } }, __props)}>
      <thead>
        <tr>
          <th class={style({
              base: {
                textAlign: "left",
                padding: "16px 24px",
                fontSize: "14px",
                fontWeight: "600",
                color: "var(--text-secondary)",
                borderBottom: "2px solid var(--border)",
                textTransform: "uppercase",
                letterSpacing: "0.5px"
              }
            }, __props)}>Feature</th>
          <th class={style({
              base: {
                textAlign: "center",
                padding: "16px 24px",
                fontSize: "16px",
                fontWeight: "600",
                color: "var(--text)",
                borderBottom: "2px solid var(--border)"
              }
            }, __props)}>{column1Title}</th>
          <th class={style({
              base: {
                textAlign: "center",
                padding: "16px 24px",
                fontSize: "16px",
                fontWeight: "600",
                color: "var(--text)",
                borderBottom: "2px solid ",
                backgroundColor: "var(--bg-100)"
              },
              tablet: {},
              mobile: {}
            }, __props)}>{column2Title}</th>
        </tr>
      </thead>
      {list(items).map((item, itemIndex) => (
        <tr>
          <td class={style({
              base: {
                padding: "16px 24px",
                fontSize: "15px",
                color: "var(--text)",
                borderBottom: "1px solid var(--border-100)"
              }
            }, __props)}>{item.title}</td>
          <td class={style({
              base: {
                textAlign: "center",
                padding: "16px 24px",
                borderBottom: "1px solid var(--border-100)"
              }
            }, __props)}>{item.value}</td>
          <td class={style({
              base: {
                textAlign: "center",
                padding: "16px 24px",
                borderBottom: "1px solid var(--border-100)",
                backgroundColor: "var(--surface)"
              }
            }, __props)}>{item.label}</td>
        </tr>
      ))}
    </table>
  </div>
</Section>
